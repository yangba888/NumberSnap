# NumberSnap 更新记录

## 0.1.4

- 延迟加载 NumPy、RapidOCR 与 OCR 工作线程，降低启动和空闲内存。
- 限制 OpenBLAS 与 ONNX Runtime 的线程池规模，减少常驻线程和内存提交量。
- OCR 忙碌时阻止重复截图任务排队，避免图片在内存中积压。
- 遮罩关闭后立即释放全屏截图，Release 模式不再构造调试日志副本。

## 0.1.3

- 将内容区“窗口置顶”复选框改为标题栏钉子按钮。
- 通过 Windows `SetWindowPos` 切换置顶，不重建 HWND，避免白屏闪烁。
- 增加自绘标题栏、最小化、最大化和真正退出程序的关闭按钮。
- 将主窗口右下角“设置”替换为“关闭”。
- 增加当前用户级开机自启；登录后以 `--startup` 静默进入托盘。

## 0.1.2

- 截图按显示器原生设备像素比裁剪，修复高 DPI 模糊。
- 支持从中文文本中提取相邻数字，不输出周围英文、汉字或空格。
- 剪贴板同时提供 TSV 和左对齐文本 HTML 表格，改善 Excel/WPS 粘贴效果。
- 全局快捷键改为由 Qt 主线程接收 Windows `WM_HOTKEY`。
- 增加单实例锁，避免多个托盘进程争抢快捷键。
- 增加可配置快捷键、清除结果和窗口置顶功能。
